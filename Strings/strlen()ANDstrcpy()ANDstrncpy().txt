/* Program to 
    1.Find length of string
    2.Copy a string into another string
    3.Copy only "n" charcaters from one string to another string
*/

#include<stdio.h>
#include<string.h>

int main()
{
    char s[40],copy[40],copy1[40];
    int l,n;
    printf("Enter the string\n ");
    scanf("%[^\n]",&s);
    l=strlen(s);
    printf("Length of %s is %d\n",s,l);
    strcpy(copy,s);
    printf("Given string is %s\n",s);
    printf("Copied string is %s\n",copy);
    printf("Enter the number of characters to be copied\n");
    scanf("%d",&n);
    strncpy(copy1,s,n);
    copy1[n]='\0';
    printf("String copied till %d charcaters is %s\n",n,copy1);
}