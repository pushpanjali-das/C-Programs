/* Program to read the employee information from the data file */
#include<stdio.h>
#include<conio.h>
int main()
{
	FILE *fp;
	char ename[20];
	int eno;
	float sal;
	clrscr();
	fp=fopen("employee.dat","r");
	fscanf(fp,"%d%s%f",&eno,&ename,&sal);	//fscanf() is used to read any kind of input from the file.
	printf("%d  %s  %f",eno,ename,sal);
	fclose(fp);
	return 0;
}