/* Program to store an integer in the file */
#include<stdio.h>
#include<conio.h>
int main()
{
	FILE *fp;
	int n;
	clrscr();
	fp=fopen("storeint.txt","w");
	printf("Enter the integer\n");
	scanf("%d",&n);
	putw(n,fp);
	fclose(fp);
	return 0;
}